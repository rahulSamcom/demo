name: iOS CI

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and Test
    runs-on: macOS-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Xcode
        uses: nightstory/setup-xcodegen@v1
        with:
          enable-cache: true
          version: latest

      - name: Install CocoaPods
        run: gem install cocoapods

      - name: Install project dependencies
        run: |
          pod install

      - name: Build and test
        run: |
          xcodebuild clean build test -workspace Firebase_Demo.xcworkspace -scheme Firebase_Demo -sdk iphonesimulator -destination "platform=iOS Simulator,OS=16.4,name=iPad Pro (12.9-inch) (6th generation)" CODE_SIGNING_REQUIRED=NO
